apiVersion: v1
kind: Service
metadata:
  name: agent-builder
  namespace: agennext
  labels:
    app.kubernetes.io/name: agent-builder
    app.kubernetes.io/part-of: agennext
    app.kubernetes.io/component: a2ui-preview
    app.kubernetes.io/managed-by: pod-manager
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-builder
    app.kubernetes.io/component: a2ui-preview
  ports:
    - name: http
      port: 3000
      targetPort: http
